% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mclustDensityFunction.R
\name{fast_gmm_density_batch}
\alias{fast_gmm_density_batch}
\title{Batched GMM density evaluation using precomputed parameters}
\usage{
fast_gmm_density_batch(X, pre)
}
\arguments{
\item{X}{Numeric matrix (n x d) or data.frame coercible to one — query points
in rows, dimensions in columns.}

\item{pre}{List of precomputed parameters from \code{precompute_gmm_params}.}
}
\value{
Numeric vector of length n with the density at each row of X.
}
\description{
Vectorized variant of \code{fast_gmm_density} for evaluating many points at
once. Operates per component on the whole batch of points, then combines via
log-sum-exp. Substantially faster than looping single-point evaluations when
the caller already has all query points in hand (e.g. plotting density rasters,
threshold sweeps, or sweep-style diagnostics in the vignettes).
}
\keyword{internal}
