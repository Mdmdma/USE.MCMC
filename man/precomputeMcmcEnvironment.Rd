% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precomputeMcmcEnvironment.R
\name{precomputeMcmcEnvironment}
\alias{precomputeMcmcEnvironment}
\title{precomputeMcmcEnvironment}
\usage{
precomputeMcmcEnvironment(
  env.data.raster = NULL,
  dimensions = c("PC1", "PC2"),
  seed.number = 42,
  precomputed.pca = NULL,
  plot_proc = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{env.data.raster}{Terra raster containing the environment.}

\item{dimensions}{Character vector of the PC dimensions to retain (e.g.
\code{c("PC1", "PC2")}). Must match the \code{dimensions} later passed to
\code{\link[=paSamplingMcmc]{paSamplingMcmc()}}.}

\item{seed.number}{Seed used for the environment subsampling so the bundle is
reproducible. Must match the \code{seed.number} later passed to
\code{\link[=paSamplingMcmc]{paSamplingMcmc()}}.}

\item{precomputed.pca}{Optional result of \code{\link[=rastPCA]{rastPCA()}} (a list with a \verb{$PCs}
element) to reuse instead of recomputing the PCA.}

\item{plot_proc}{If TRUE, \code{mclust} draws its fit diagnostics.}

\item{verbose}{If TRUE, prints progress.}
}
\value{
A list (S3 class \code{mcmc_environment}) with elements \code{pcs_packed},
\code{env.with.pc.sf}, \code{env.data.cleaned}, \code{environmental.data.model},
\code{environmental.densities}, \code{covariance.matrix}, \code{distance.threshold},
\code{dimensions}, \code{seed.number} and \code{rng_state}. Pass it to
\code{\link[=paSamplingMcmc]{paSamplingMcmc()}}'s \code{precomputed.env} argument.
}
\description{
Runs the \emph{environment-only} portion of \code{\link[=paSamplingMcmc]{paSamplingMcmc()}} once and returns a
reusable, serialisable bundle. Everything this function computes depends only
on the environmental raster (plus \code{dimensions}, \code{seed.number} and the
subsampling), and is therefore \strong{constant across presence sets} — so when the
same environment is sampled for many species / presence draws, this work can
be done a single time and handed back to \code{\link[=paSamplingMcmc]{paSamplingMcmc()}} via its
\code{precomputed.env} argument.
}
\details{
Cached steps: the PCA (\code{rastPCA}), construction of the PC-augmented background
data frame, the Gaussian-mixture fit to the environment and its density
evaluation over the full background, the proposal covariance matrix, and the
nearest-neighbour distance threshold. The species GMM, the combined density
function and the MCMC chains themselves remain per-call in
\code{\link[=paSamplingMcmc]{paSamplingMcmc()}}.

The bundle is safe to \code{saveRDS()} / \code{readRDS()}: the PC SpatRaster is stored
wrapped via \code{\link[terra:wrap]{terra::wrap()}} (terra objects are external pointers and would not
otherwise survive serialisation) and unwrapped on use. This makes the bundle
usable across separate R sessions / batch jobs.

Reproducibility: the function seeds with \code{seed.number} and captures the RNG
state immediately after the environment block. \code{\link[=paSamplingMcmc]{paSamplingMcmc()}} restores
that state before drawing the species model and chains, so results are
\strong{identical} whether the environment was computed inline or loaded from a
cached bundle.
}
