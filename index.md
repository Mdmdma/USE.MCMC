# Development Repository

## This is a development repository

It forks the [USE package](https://github.com/danddr/USE) with the goal
to extend it to enable MCMC sampling in higher dimensional spaces.

The repo can be downloaded by using the following command in a terminal
or downloading the repo as a zip file:

    git clone git@github.com:Mdmdma/USE.MCMC.git
