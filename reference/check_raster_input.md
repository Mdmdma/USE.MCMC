# Check that a raster input is valid

Check that a raster input is valid

## Usage

``` r
check_raster_input(x, arg_name = "env.rast")
```

## Arguments

- x:

  The object to check

- arg_name:

  Name of the argument (for error messages)
