# Check that a spatial points input is valid

Check that a spatial points input is valid

## Usage

``` r
check_spatial_points(x, arg_name = "pres", allow_null = FALSE)
```

## Arguments

- x:

  The object to check

- arg_name:

  Name of the argument (for error messages)

- allow_null:

  If TRUE, NULL values are accepted without error
